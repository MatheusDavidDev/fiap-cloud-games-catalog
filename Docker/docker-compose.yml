version: "3.9"

name: "fgc-catalog-docker"
services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2025-latest
    container_name: FCG-catalog-sql-database
    ports:
      - "1433:1433"
    environment:
      SA_PASSWORD: "Sql@123456789"
      ACCEPT_EULA: "Y"
      MSSQL_PID: "Developer"
    volumes:
      - sqlserver_data:/var/opt/mssql
    restart: unless-stopped

volumes:
  sqlserver_data: